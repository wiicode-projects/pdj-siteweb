name: PLAT DU JOUR SITE WEB CI/CD

on:
  push:
    branches:
      - dev
      - main
      - infra/devops
  pull_request:
    branches:
      - dev
      - main

  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: sitepdj
  STAGING_PORTAL_URL: https://pdj-backoffice-dev.wiicode.tech
  PROD_PORTAL_URL: https://backoffice.leplatdujour.ch

jobs:

  # ============================================================
  # JOB 1 — BUILD & PUSH
  # ============================================================
  build:
    name: Build & Push Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate short SHA
        id: vars
        run: echo "SHA_SHORT=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Set Vite build env
        id: vite
        run: |
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "app_env=production" >> "$GITHUB_OUTPUT"
            echo "portal_url=${{ env.PROD_PORTAL_URL }}" >> "$GITHUB_OUTPUT"
          else
            echo "app_env=staging" >> "$GITHUB_OUTPUT"
            echo "portal_url=${{ env.STAGING_PORTAL_URL }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build & Push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: ${{ github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/main' }}
          target: production
          build-args: |
            VITE_APP_ENV=${{ steps.vite.outputs.app_env }}
            VITE_RESTAURANT_PORTAL_URL=${{ steps.vite.outputs.portal_url }}
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.SHA_SHORT }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:latest
          labels: |
            org.opencontainers.image.title=sitepdj

  # ============================================================
  # JOB 2 — DEPLOY STAGING
  # Uniquement sur dev
  # ============================================================
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/dev'
    environment:
      name: staging

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Get short SHA
        id: vars
        run: echo "SHA_SHORT=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Copy compose file to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          source: "docker/docker-compose.staging.yml, scripts/deploy-staging.sh"
          target: ${{ vars.DEPLOY_PATH }}
          strip_components: 1

      - name: Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            echo "${{ secrets.GHCR_PAT }}" | docker login ghcr.io \
              -u "${{ github.repository_owner }}" --password-stdin

            cd ${{ vars.DEPLOY_PATH }}
            mv docker-compose.staging.yml docker-compose.yml
            chmod +x deploy-staging.sh
            bash deploy-staging.sh ${{ steps.vars.outputs.SHA_SHORT }}


            # IMAGE_TAG=${{ steps.vars.outputs.SHA_SHORT }} \
            #   docker compose up -d --pull always sitepdj

           

  # ============================================================
  # JOB 3 — DEPLOY PRODUCTION
  # Uniquement sur main (merge depuis dev)
  # ============================================================
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    environment:
      name: production

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Get short SHA
        id: vars
        run: echo "SHA_SHORT=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Copy compose file to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          source: "docker/docker-compose.production.yml, scripts/deploy-production.sh"
          target: ${{ vars.DEPLOY_PATH }}
          strip_components: 1

      - name: Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            echo "${{ secrets.GHCR_PAT }}" | docker login ghcr.io \
              -u "${{ github.repository_owner }}" --password-stdin

            cd ${{ vars.DEPLOY_PATH }}
            mv docker-compose.production.yml docker-compose.yml
            chmod +x deploy-production.sh
            bash deploy-production.sh ${{ steps.vars.outputs.SHA_SHORT }}